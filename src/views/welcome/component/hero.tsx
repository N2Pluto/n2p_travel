import { Grid } from '@mui/material'
import CardText from './text'

const Hero = () => {
  return (
    <Grid container spacing={2}>
      <Grid item xs={12}>
        <CardText />
      </Grid>
    </Grid>
  )
}

export default Hero
